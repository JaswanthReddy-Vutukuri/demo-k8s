apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: favorites-data
  name: favorites-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
